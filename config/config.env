# Toyota Diag — Connection Configuration
# Copy to ~/.config/toyota-diag/config.env and edit for your setup.

# Serial port for OBD-II adapter
# macOS USB (FTDI/OBDLink EX):  /dev/cu.usbserial-XXXX
# macOS USB (CP210x):           /dev/cu.SLAB_USBtoUART
# macOS Bluetooth (OBDLink MX): /dev/cu.OBDLinkMX-XXXX
# Linux USB:                    /dev/ttyUSB0
# Emulator (TCP):               socket://localhost:35000
SERIAL_PORT="auto"

# Baud rate (usually auto-detected; 38400 for most ELM327, 115200 for STN/OBDLink)
BAUD_RATE=""

# Vehicle profile — filename (without .json) from config/vehicles/
VEHICLE="rav4_xa50"

# Connection timeout in seconds
TIMEOUT=10

# Output format: json | human
OUTPUT="json"
